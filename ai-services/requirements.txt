# Minimal development requirements for ai-services
# Trimmed to allow local Docker builds during development

fastapi==0.109.0
uvicorn[standard]==0.27.0
httpx==0.26.0
requests==2.31.0
python-multipart==0.0.6

# Image processing + numerics
opencv-python-headless==4.9.0.80
numpy==1.26.3
Pillow==10.2.0

# Utilities
loguru==0.7.2
aiofiles==23.1.0

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0
motor==3.3.2
pymongo==4.6.1
redis==5.0.1

# Keep pytest for local testing
pytest==7.4.4

# NOTE: Full ML deps (torch, transformers, etc.) were removed for faster local builds.
# Re-add them in CI or production images as needed.
